import React, { useState } from "react";

const Form = () => {
  const initialData = {
    
    name: "",
    email: "",
    phone: "",
    message: "",
  };
  const [contactInfo, setContactInfo] = useState({});
  return <form></form>;
};

export default Form;
